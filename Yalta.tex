% \documentclass[12pt,a4paper,draft]{article}
\documentclass[12pt,a4paper]{article}

\setlength{\textwidth}{170mm} %ширина текста 6in=152mm
\setlength{\textheight}{255mm}
\setlength{\oddsidemargin}{5mm} %расстояние между левым краем листа и левым краем
                                %текста на правой странице минус 1in
\setlength{\topmargin}{-20mm} %расстояние между верхним краем листа и верхним
                            %колонтитулом минус 1in
%\footheight 4mm %высота нижнего колонтитула
%\footskip 5mm %расстояние между нижним краем текста и нижнем краем
               %нижнего колонтитула


\usepackage[cp1251]{inputenc}
\usepackage[russian]{babel}
%\usepackage{srcltx}

\usepackage{amsmath,bm}

\usepackage{mathtext}
\usepackage[small,centerlast]{caption2}
\renewcommand{\captionlabeldelim}{.}
\usepackage{indentfirst}
\usepackage{amssymb}
%\usepackage{natbib}
\usepackage{footnote}
\usepackage{afterpage}
\usepackage{cite}

\usepackage[dvips]{graphicx,color}
%\usepackage{graphicx}
\graphicspath{{Figures/}}

\newcommand{\suml}{\sum\limits}
\newcommand{\intl}{\int\limits}

\title{Simulation of liquid cube  fracture with SPH}
\author{M N Davydov}
\date{Lavrentyev Institute of Hydrodynamics of the Siberian Branch of the Russian Academy of Sciences // davydov@hydro.nsc.ru}


\begin{document}

\maketitle


\begin{abstract}
В работе численно исследуется задача о разрушении жидкого кубика, внутри которого имеется полость с высоким давлением, с использованием метода SPH (Smoothed Particle Hydrodynamics)~\cite{Monaghan2005}. Показана деформация полости, и формирование выброса на верхней грани кубика. Численное моделирование показывает что распространение  ударной волны от растущей полости и ее отражение от стенок приводит к релаксации поля давления и дальнейшему инерционному разлету среды.  
\end{abstract}

\section{Introduction}

Известно что разрушение жидкостей, таких как обычная вода или магматические расплавы, носит кавитационный характер и связаны с наличием большого количества пузырьков газа~\cite{Davydov2012a}. Обычные численные методы, основанные на конечных разностях, не в состоянии решить подобные задачи, следовательно требуется использовать методы, способные провести моделирование течений в области со сложной изменяющейся геометрией	и меняющейся топологией самого течения. В данной работе рассматривается модельная задача о разрушении жидкого объема с использованием метода SPH. 

\section{SPH method}

The smoothed particle hydrodynamics (SPH) method~\cite{Monaghan1992,Monaghan2005} is an effective meshless Lagrangian numerical
method used to calculate flow structures with an unknown free boundary, in particular, high-velocity processes in
media with the modeled object topology appreciably changed under intense dynamic loading.

In this method, $N$ spherical particles are placed into the modeled physical space. Each particle has a
mass $m_i$, internal energy $e_i$, and velocity $\vec{v}_i$. The particles move in accordance with the laws of mechanics. If these
physical quantities are known at a certain time at all points $j = 1,\ldots{},N$ to which $N$ particles are placed, i.e., if
a certain function $f(r_j)$ is specified, then its value at an arbitrary point of the modeled space can be obtained by
means of discretization of the interpolation formula
\[ \left\langle f(r) \right\rangle = \int f(r') W(r-r', h) dr,\]
where $h$ is the smoothing radius and $W(r-r', h)$ is the smoothing function (kernel) for which the following conditions
should be satisfied:
\[ \int W(r,h) dr = 1, \qquad   \lim_{h \to 0} W(r,h)=\delta(r)\]
In this work, we use the known definition of the kernel based on third-power piecewise-spline functions~\cite{Monaghan1992}.	

As the smoothing function $W$ is not equal to zero only in a certain (small) neighborhood of the point with the
coordinate $r$, the result of interpolation is affected only by those nodes that are located in the neighborhood of
smoothing of this point. Therefore, it is possible to avoid summation over all known values of the function and to
perform it only over the neighboring nodes (particles) located at distances smaller than $2h$.

It should be noted that the procedure of sampling all particles in space has a quadratic order of complexity,
and it is next to impossible to implement it in practice. As the particles arbitrarily move in space and can freely
mix, the task of effective (time-efficient) searching for the neighboring particles is extremely important for the
SPH method. One possible approach to solving this problem is to divide the space into cells and to perform
summation only over particles in the neighboring cells. It should be noted that the mesh constructed for this
purpose is an auxiliary tool used for accelerating the process of searching for the neighboring particles rather than
for approximation; therefore, it does not affect the resultant solution, and the SPH method is still a meshless one.
	
	In the SPH method, the gas dynamic equations have the form
	\[  \frac{\partial \rho_i}{\partial t} = - \rho_i \suml_{j=1}^{N} \frac{m_j}{\rho_j} (v_j -  v_i) \nabla W(r_i - r_j,h)
\]
\[\frac{\partial v_i}{\partial t} = - \suml_{j=1}^{N} m_j \left ( \frac{p_i}{\rho_i^2} + \frac{p_j}{\rho_j^2} \right) \nabla W(r_i-r_j,h).\]

We use the known definition of the liquid state equation:  
\[p= p_0 + \frac{\rho_0 c_0^2}{\gamma} \left[ \left( \frac{\rho}{\rho_0} \right)^{\gamma} - 1 \right],\]
where  $\gamma = 7.15$, $\rho_0$ and $p_0$ is a reference density and pressure, $c_0$ is a speed of sound (approx 1500 m/s).

			
\section{Постановка задачи}

В начальный момент времени жидкость представляла собой кубик со стороной 1 см, расположенный на плоскости XY, ось OZ проходит через центр кубика. Внутри жидкости, на расстоянии 2.5 мм от верхней грани куба, находилась сферическая полость радиуса 1мм, центр которой расположен на оси OZ. Давление в полости оставляло 10000 атм, остальной объем жидкости был при атмосферном давлении. На рисунках ниже показано распределение давления в узлах и скоростей частиц в центральном сечении кубика (плоскость $y=0$).


\begin{figure}
\begin{center}
\includegraphics[width=0.6\textwidth]{P-1.eps}\includegraphics[width=0.4\textwidth]{Vel-1.eps}
\end{center}
\caption{\label{P-Vel-1} Распределение давления и скорости в центральном сечении кубика в момент времени 1 мкс}
\end{figure}

От полости высокого давления распространяется ударная волна, амплитуда которой естественно уменьшается по мере распространения. В указанный момент времени волна подходит в верхней грани кубика, начинается процесс отражения (Рис.~\ref{P-Vel-1}). Ближний к полости слой жидкости приходит в движение, на графике  становятся заметны скорости частиц (частицы в которых $\vec{v} =0$ не показаны). Хорошо видно что распределение скоростей симметрично относительно полости. 

\begin{figure}
\begin{center}
\includegraphics[width=0.6\textwidth]{P-5.eps}\includegraphics[width=0.4\textwidth]{Vel-5.eps}
\end{center}
\caption{\label{P-Vel-5} Распределение давления и скорости в центральном сечении кубика в момент времени 5 мкс}
\end{figure}

В дальнейшем волна доходит до краев кубика и отражается от его границ, сначала это происходит на верхней грани, потом на боковых и нижней (Рис.~\ref{P-Vel-5}). К этому времени верхняя грань заметно деформировалась, сдвинувшись вверх. Почти все частицы пришли в движение, видно что частицы на границе полости <<толкают>> остальные частицы.

\begin{figure}
\begin{center}
\includegraphics[width=0.6\textwidth]{P-25.eps}\includegraphics[width=0.4\textwidth]{Vel-25.eps}
\end{center}
\caption{\label{P-Vel-1} Распределение давления и скорости в центральном сечении кубика в момент времени 25 мкс}
\end{figure}

Через несколько десятков микросекунд поле давления практически релаксирует, только в отдельных частицах давление больше $p_0$, а набравшие скорость частицы продолжают разлетаться по инерции, тем самым моделируя разрушение среды (Рис.~\ref{P-Vel-25}). Становятся заметными деформации боковых граней. Распределение частиц показывает что внутри кубика формируются зоны разрежения, рост которых приведет к разрушению среды на отдельные фрагменты. 




\begin{figure}
\begin{center}
\includegraphics[width=0.4\textwidth]{3D-1.eps} \hfill \includegraphics[width=0.4\textwidth]{3D-2.eps}
\end{center}
\caption{\label{3D-1} Изоповерхность $\rho=0.45$ $\text{g}/\text{cm}^3$ для половины кубика в моменты времени 500 и 3000 мкс}
\end{figure}

На Рис.~\ref{3D} показано развитие процесса в объемном виде. Изображена изоповерхность плотности для «половинки» кубика, построенная по значению 0.45 г/см3, то есть изоповерхность ограничивает область, в которой плотность среды больше или равна заданной. Хорошо заметно, что к моменту времени 300 мкс (Рис.~\ref{3D}) полость расширилась в несколько раз, «вытянулась» вверх, выталкивая слои жидкости, образующие верхнюю грань кубика. Нижняя и боковые грани куба деформировались незначительно, а на верхней грани формируется заметная выпуклость, появляется отверстие. 

В дальнейшем на верхней грани кубика формируется вертикальная струя жидкости, которая постепенно разрушается. На боковых и нижней гранях, образуются отдельные отлетающие слои, где разрушение пока не настолько сильно выражено. В дальнейшем и на этих участках будет происходить разрушение жидкости и возникновение отколовшихся от основного объема «слоев». Видно, что среда, разлетаясь, теряет связность и распадается на отдельные фрагменты. Дальнейшее развитие процесса приводит к распылению частиц и окончательному разрушению среды.

Необходимо отметить, что SPH частицы, отлетая друг от друга на расстояние, большее чем радиус сглаживания, перестают влиять друг на друга, что можно интерпретировать как разрушение среды~\cite{DavydovKedrinskii2013}. Так к моменту времени 300 мкс частицы, ранее расположенные над полостью, фактически распылились, и в верхней части объема образовалось отверстие.







\section{Заключение}

The results of numerical modeling show that the SPH method can be used to study the dynamics of the state of a  flow of a medium with a liquid cube fracture. The results of the numerical analysis of the processes of formation and dynamics of the structure of spalling zones after reflection of the shock wave generated by an underwater explosion from the free surface are presented. A numerical study of the 
dynamics of the liquid cube under shock-wave loading shows that the shock wave reflected from the free
surface leads to the relax of a high pressure and subsequent decomposition into individual fragments and clusters
of almost free particles.

This work was supported by the
Russian Foundation for Basic Research (Grant No. 15-05-03336).


%\bibliographystyle{unsrt}
\bibliographystyle{gost2008}
\bibliography{d:/Dropbox/BibtexBase/Work}


\end{document}